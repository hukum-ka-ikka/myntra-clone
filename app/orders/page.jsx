import React from "react";
import Order from "../components/order/Order";

export const metadata = {
  title: "Orders",
};

const page = () => {
  return (
    <div>
      <Order />
    </div>
  );
};

export default page;
